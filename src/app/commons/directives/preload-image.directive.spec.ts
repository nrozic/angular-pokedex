import { PreloadImageDirective } from './preload-image.directive';

describe('PreloadImageDirective', () => {
  it('should create an instance', () => {
    const directive = new PreloadImageDirective();
    expect(directive).toBeTruthy();
  });
});
